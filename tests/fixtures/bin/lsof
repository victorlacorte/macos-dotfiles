#!/usr/bin/env bash

[ "${AGENT_TEST_LSOF_FAIL:-0}" = 0 ] || exit 127

pid=''
while [ "$#" -gt 0 ]; do
  if [ "$1" = -p ]; then
    pid="${2:-}"
    break
  fi
  shift
done

case "$pid" in
200) printf '%s\n' "${AGENT_TEST_LSOF_200:-}" ;;
202) printf '%s\n' "${AGENT_TEST_LSOF_202:-}" ;;
esac
